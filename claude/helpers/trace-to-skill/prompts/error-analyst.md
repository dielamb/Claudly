You are analyzing failed AI agent runs to find failure patterns.

Given these execution traces (feedback: bad):
{TRACES}

Identify patterns in what went wrong. For each pattern:
RULE: [what to avoid] | EVIDENCE: [run numbers] | PRIORITY: [high/med/low]

Focus on: wrong tool sequences, missing checks, incorrect assumptions, constraint violations.
Output ONLY rule lines, no explanation.
