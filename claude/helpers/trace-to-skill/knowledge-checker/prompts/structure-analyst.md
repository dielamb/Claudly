You are analyzing memory retrieval test run patterns for structural insights.

Given these execution traces (all runs):
{TRACES}

Analyze keyword match sequences and context injection patterns. For each pattern:
RULE: [structural observation] | EVIDENCE: [run numbers] | PRIORITY: [high/med/low]

Focus on: domain match patterns, RuFlo hit rates, skip-rate by word count.
Output ONLY rule lines, no explanation.
